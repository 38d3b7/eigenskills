# Auto-injected by EigenCompute KMS — do NOT set manually
# MNEMONIC=

# EigenAI Grant API (default endpoint, no API key needed — uses TEE wallet signature)
EIGENAI_GRANT_API=https://determinal-api.eigenarcade.com

# Skill registry URL (GitHub raw URL to registry.json)
SKILL_REGISTRY_URL=https://raw.githubusercontent.com/yourorg/eigenskills-registry/main/registry/registry.json

# Visible on Verifiability Dashboard
NETWORK_PUBLIC=sepolia

# Optional: user-provided API keys for skills
# OPENAI_API_KEY=your_key
# TWITTER_API_KEY=your_key
